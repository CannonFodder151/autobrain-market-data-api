# market-data-api secrets — NEVER commit real values.
# In dev, point API_KEY_FILE at a local file containing the key, or mount it
# as a Docker secret at /run/secrets/market_data_api_key.
# API_KEY_FILE=/path/to/api_key.txt
